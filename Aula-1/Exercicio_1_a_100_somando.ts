export function exercicio1A100Somando() {
    let contador = 1
    let resultado = 0
    while (contador <= 100) {
        resultado = resultado + contador
        contador++
        console.log(resultado);
    }
}